/**
 * @file Manage realms.
 */

/**
 * Add custom DNS domain to a realm.
 * @type {import('../types/bindings/Realm').AddCustomDomain}
 */
export const $addCustomDomain = ({ state }) => {
  state;
};

/**
 * Get information about a realm.
 * @type {import('../types/bindings/Realm').Info}
 */
export const $info = ({ state }) => {
  state;
};

/**
 * List all realms.
 * @type {import('../types/bindings/Realm').List}
 */
export const $list = ({ state }) => {
  state;
};

/**
 * Remove custom DNS domain to a realm.
 * @type {import('../types/bindings/Realm').RemoveCustomDomain}
 */
export const $removeCustomDomain = ({ state }) => {
  state;
};
