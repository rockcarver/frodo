import { state } from "../..//state"
/**
 * Get an access token using client credentials grant type.
 * @type {import('../types/bindings/Token').GetAccessToken}
 */
const getAccessToken = ({ state }) => {};

getAccessToken({state})