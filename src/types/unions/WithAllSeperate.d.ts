/**
 * Import all the journeys/trees from separate files (*.json) in the current directory
*/
export interface WithAllSeperate {
    /**
     * Import all the journeys/trees from separate files (*.json) in the current directory
    */
    allSeperate: boolean;
}